const root = require("./root");
const user = require("./user");
const game = require("./game");
const chat = require("./chat");
const lobby = require("./lobby");

module.exports = { root, user, game, chat, lobby };
